1)	Рассказать о том, как избегать 100500 ифов
2)	Почему, в виде мы не пишем use Yii;
    <?php foreach ($list as $item): ?>
    <h1><a href="<?php echo Yii::$app->urlManager->createUrl(['test/view', 'id' => $item['id']]); ?>"><?php echo $item['title'];?></a></h1>
    <p><?php echo $item['content'];?></p>
    <hr>
    <?php endforeach; ?>
3)	Я не умею документировать код
4)	Рекурсия
5)      Несколько news

